<Page x:Class="PCAdminApp.Pages.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:PCAdminApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReportPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="3">
            <TextBlock Text="Создание отчетов" Foreground="Black" FontWeight="Bold" FontSize="24" Margin="0 20 0 0" />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <DataGrid Name="ClientsDG" AutoGenerateColumns="False" ItemsSource="{Binding Client}" Visibility="Collapsed">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=FullName}" x:Name="DGFullName" Header="ФИО" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=PhoneNumber}" x:Name="DGPhoneNum" Header="Номер телефона" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=BalanceRUB}" x:Name="DGBalanceRUB" Header="Баланс в рублях" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=BalanceBonus}" x:Name="DGBonusBalance" Header="Бонусный баланс" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.RowSpan="3" Margin="5 -50 5 0" VerticalAlignment="Center">
            <TextBlock Text="Создать отчет о клиентах" Margin="0 20 0 0" HorizontalAlignment="Center" FontSize="16" Foreground="Black" FontWeight="Bold"/>
            <TextBlock TextWrapping="Wrap" Margin="0 0 0 10">
                Данный отчет хранит следующую информацию: <LineBreak/>
                1) ФИО клиентов;<LineBreak/>
                2) Номера телефонов клиентов;<LineBreak/>
                3) Игровой баланс;<LineBreak/>
                4) Бонусный баланс.
            </TextBlock>
            <Button x:Name="BtnClientReport" Click="BtnClientReport_Click" Content="Создать отчет" Height="25" FontSize="14" BorderThickness="0" Background="#E82E39" Foreground="White" FontWeight="DemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <DataGrid Name="UsersDG" AutoGenerateColumns="False" ItemsSource="{Binding User}" Visibility="Collapsed">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=FullName}" x:Name="DGFullNameUser" Header="ФИО" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=UserName}" x:Name="DGUserName" Header="Логин" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=Password}" x:Name="DGPassword" Header="Пароль" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Path=Role.Name}" x:Name="DGRole" Header="Роль" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" VerticalAlignment="Center" Margin="5 -50 5 0">
            <TextBlock Text="Создать отчет о сотрудниках" Margin="0 20 0 0" HorizontalAlignment="Center" FontSize="16" Foreground="Black" FontWeight="Bold"/>
            <TextBlock TextWrapping="Wrap" Margin="0 0 0 10">
                Данный отчет хранит следующую информацию: <LineBreak/>
                1) ФИО сотрудника;<LineBreak/>
                2) Логин сотрудника;<LineBreak/>
                3) Пароль сотрудника;<LineBreak/>
                4) Роль сотрудника.
                    </TextBlock>
            <Button x:Name="BtnUserReport" Click="BtnUserReport_Click" Content="Создать отчет" Height="25" FontSize="14" BorderThickness="0" Background="#E82E39" Foreground="White" FontWeight="DemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Page>
