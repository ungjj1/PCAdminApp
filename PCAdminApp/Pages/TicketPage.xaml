<Page x:Class="PCAdminApp.Pages.TicketPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PCAdminApp.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TicketPage">

    <Page.Resources>
        <DataTemplate x:Key="TicketTemplate">
            <Border BorderBrush="LightGray" BorderThickness="1" Margin="0 5" Padding="10 5" MouseLeftButtonUp="Border_MouseLeftButtonUp" Height="auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock FontSize="16">
                            <Run Text="Название устройства:" FontWeight="DemiBold"/>
                            <Run Text="{Binding Computer.PCName}"/>
                        </TextBlock>
                        <TextBlock Width="300 " TextWrapping="Wrap" FontSize="16">
                            <Run Text="Описание тикета:" FontWeight="DemiBold"/>
                            <Run Text="{Binding Description}"/>
                        </TextBlock>
                        <TextBlock FontSize="16">
                            <Run Text="Дата создания тикета:" FontWeight="DemiBold"/>
                            <Run Text="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy\}}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TicketList" ItemTemplate="{StaticResource TicketTemplate}"/>
        </ScrollViewer>
    </Grid>
</Page>
