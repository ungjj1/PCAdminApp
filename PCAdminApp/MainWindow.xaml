<Window x:Class="PCAdminApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCAdminApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0">
            <!-- Здесь будет логотип-->
        </Border>
        <Border Grid.Column="0" Grid.Row="1" Background="#3D4E63">
            <StackPanel Margin="0 20 0 0">
                <Button Name="BtnComputers" Click="BtnComputers_Click"  Style="{StaticResource MenuButtonStyle}" Content="Компьютеры" Margin="10 0 10 0" Width="170"/>
                <Button Name="BtnTicket" Click="BtnTicket_Click"  Style="{StaticResource MenuButtonStyle}" Content="Заявки" Margin="10 0 10 0" Width="170"/>
                <Button Name="BtnInventory" Click="BtnInventory_Click" Style="{StaticResource MenuButtonStyle}" Content="Склад" Margin="10 0 10 0" Width="170"/>
                <Button Name="BtnReport" Click ="BtnReport_Click" Style="{StaticResource MenuButtonStyle}" Content="Отчеты" Margin="10 0 10 0" Width="170"/>
            </StackPanel>
        </Border>
        <Border Grid.Column="1" Grid.Row="0" Background="#3D4E63">
            <Grid Margin="20,0">
                <TextBlock x:Name="TxtPageTitle" VerticalAlignment="Center" 
                           FontSize="18" Foreground="White" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Name="TBRole" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="Выход" Background="#3D4E63" Foreground="White" Width="60" Height="30" Cursor="Hand" Name="BtnExit" Click="BtnExit_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Column="1" Grid.Row="1" Margin="10"/>
    </Grid>
</Window>